language: cpp

sudo: required

compiler:
- clang

os: linux
addons:
    apt:
        sources:
            - llvm-toolchain-precise-3.7
            - ubuntu-toolchain-r-test
        packages:
            - clang-3.7
            - ocl-icd-opencl-dev
            - libgdal-dev

install: ./travis.sh

script: 
- cd build && make